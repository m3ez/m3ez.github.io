name: Verify redesign regression contracts

on:
  push:
    branches:
      - agent/research-credentials-redesign

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run redesign contracts
        run: node --test tests/redesign.test.mjs
