name: Verify Medium hero link

on:
  push:
    branches:
      - agent/add-medium-hero-link

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run portfolio tests
        run: node --test tests/*.test.mjs
