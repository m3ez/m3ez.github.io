name: Verify PR21 final

on:
  push:
    branches:
      - agent/formalize-hero-proof-links

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run portfolio contracts
        run: node --test tests/redesign.test.mjs tests/mobile-responsive.test.mjs tests/recognition.test.mjs tests/cve-progressive-disclosure.test.mjs tests/hero-proof-links.test.mjs
