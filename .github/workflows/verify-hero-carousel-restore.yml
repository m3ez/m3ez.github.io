name: Verify hero carousel restore

on:
  push:
    branches:
      - agent/restore-hero-credential-carousel

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run redesign contracts
        run: node --test tests/redesign.test.mjs
